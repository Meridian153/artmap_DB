# 방법 1: DATABASE_URL 직접 설정 (Neon 등 외부 DB)
# DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# 방법 2: 개별 환경변수 (DATABASE_URL 미설정 시 자동 조합)
DB_HOST=
DB_PORT=5432
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_SSLMODE=
